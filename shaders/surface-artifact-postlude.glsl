/* End of the surface-only guards.
   Only the local Tectonics remap was defined; texture() is no longer
   hijacked so nothing needs undef for it. gSeamNear stays the real terrain
   diagnostic global. */
#undef uTect

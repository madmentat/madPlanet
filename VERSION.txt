VERSION 0.5.97
Base: 0.5.96
Changes: Ice / cryosphere islands looked like Malevich geometry (cubes, triangles, non-closing coasts) independent of Tectonics. Root cause: surface-artifact-prelude.glsl re-binarized continuous cubemap coverage with smoothstep(0.5±fwidth), exposing the underlying cube-face grid as hard polygons.
Fix:
  - cryoSurfaceTextureAA now keeps continuous coverage (soft heel only, no 0.5 threshold)
  - CRYO_GPU_UPSCALE 5→7 and slightly softer visualCoverage thresholds in cryosphere-gpu.js
Snapshots: snapshot/0.5.96-pre-cryo-soft is the rollback point.
Release rule: every new archive increments patch version by 0.0.1 and is named madPlanet-X.Y.Z.zip.

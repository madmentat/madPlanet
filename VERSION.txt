VERSION 0.5.27
Base: 0.5.26
Changes: draft rendering is now the default behind an inverted "Detail" toggle; toggle label no longer wraps away from its switch; compile status became a bar with a timer and a progress estimate learned from the previous run.
Release rule: every new archive increments patch version by 0.0.1 and is named madPlanet-X.Y.Z.zip.

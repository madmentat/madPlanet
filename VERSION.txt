VERSION 0.5.153
Base: 0.5.152
Changes: River display without lattice staircases.
  - js/river-gpu.js (model 13): channels are painted as whole chains (headwater to mouth, visual branches end to end); the 4-neighbour lattice polyline is corner-cut twice with Chaikin's algorithm before the Catmull-Rom trace, the two-octave meander is anchored at nodes so segments join without kinks and scales with segment length; a one-texel ridge that grows only with discharge (up to ~1.4 texels). 0.5.152 painted every graph edge separately, which kept the 500 km right angles of the synoptic grid and made every river the same blocky width
  - shaders/surface.glsl: the drainage map is read through a two-octave noise warp (10 km wiggles inside 40 km meanders) and the trunk width band varies along the channel
Regression: update river-physics and river-visual-tributaries tests for the chain painter.
Snapshot: 0.5.152 remains the rollback point before the chain-smoothed river display.
Release rule: every new archive increments patch version by 0.0.1 and is named madPlanet-X.Y.Z.zip.

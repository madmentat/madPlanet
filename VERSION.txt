VERSION 0.5.168
Base: 0.5.167 Random snowball-attractor workaround.
Changes: replace the unstable current-T_a orbital feedback with a temperate new-world initializer.
  - 0.5.167 could produce T_a about -84 C while T_f exceeded +300 C because it kept moving the orbit inward while the finite-time Weather Core remained on a cold branch.
  - Random now chooses orbital distance from the equilibrium climateModel() target (+14..+24 C) rather than chasing instantaneous T_a.
  - After the final orbit is accepted, the brand-new Weather Core is initialized from that temperate climate state.
  - Transient snow, land ice and sea ice stores are cleared only for the newly generated Random world; ordinary simulation and manually edited worlds keep their physical history.
  - Land/SST/air thermal reservoirs are seeded consistently before the first rendered frame and the radiating skin is refreshed.
  - Diagnostic state remains available at window.__madPlanetHabitableRandomThermalFix.last: targetC, formalC, actualC and AU.
Regression: Random must never use current T_a as the orbital search signal; formal orbit solve precedes fresh Weather Core initialization.
Release rule: every new archive increments patch version by 0.0.1 and is named madPlanet-X.Y.Z.zip.
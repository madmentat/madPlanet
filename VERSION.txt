VERSION 0.5.94
Base: 0.5.93
Changes: Coastline and peninsula silhouettes still showed razor cuts independent of Tectonics (seeds 8127344, 33018067). Explicit finite differences — even Frisvad ONB with central samples — can invent a planar cut when the tangent frame or sample offsets mis-align with the raster. Continent FBM itself is continuous.
Fix in surface.glsl:
  - normals from screen-space dFdx/dFdy of the centre height and of n0 (no extra terrain samples, no explicit tangent basis)
  - coast AA from fwidth(h) instead of FD gradH
localTectSupport bump gate retained. Pause/zoom unchanged.
Validation: seeds 8127344 and 33018067 at Tectonics 0% and 100%; peninsula/island silhouettes must no longer be truncated by a straight line.
Snapshots: snapshot/0.5.93-pre-screen-normals is the rollback point.
Release rule: every new archive increments patch version by 0.0.1 and is named madPlanet-X.Y.Z.zip.

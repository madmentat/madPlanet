VERSION 0.5.174
Base: exact 0.5.173 / historical 0.5.147 behaviour.
Changes: one surgical river-topology experiment; no other subsystem changes.
  - Preserve the exact 0.5.147 Catmull-Rom, FBM meanders, widths and tributary artwork.
  - Add a small soft dilation of the already-existing physical Catmull river corridor (about two river cubemap texels).
  - The old procedural river is visible only inside that soft guide; the physical corridor itself is still never drawn as the river.
  - Goal: break closed FBM contour loops and long coast-to-coast pseudo-rivers while keeping the 0.5.147 visual style.
  - This deliberately does NOT use the narrow 0.5.148 riverGeomProc*physRiverHalo gate and does NOT add coastline clipping, vector/chord rivers, fine drainage or climate changes.
  - Weather, water, vegetation, clouds, Random generation and river physics remain byte-for-byte 0.5.147 except for this surface-shader visibility rule.
Manual regression seed: 87707715 from the 0.5.173 screenshots.
Release rule: every new archive increments patch version by 0.0.1 and is named madPlanet-X.Y.Z.zip.
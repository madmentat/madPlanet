VERSION 0.5.26
Base: 0.5.25
Changes: removed the biome texture atlas entirely; named URL hash format (v4); visible starfield toggle replacing the textures one.
Release rule: every new archive increments patch version by 0.0.1 and is named madPlanet-X.Y.Z.zip.

VERSION 0.5.134
Base: 0.5.133
Changes: Split the top-left thermometer into immediate actual temperature T_a and equilibrium forecast T_f.
  - replace the ambiguous single T-bar readout with T_a, the current cubed-sphere area-weighted Weather Core surface temperature, plus a separate T_f forecast/equilibrium row underneath
  - force one Weather Core initialization for the headline thermometer when needed, so startup never temporarily displays climateModel().C in the slot reserved for current observed temperature
  - keep T_f explicitly secondary and label it as the expected settled climate regime rather than a current measurement
  - colour T_a with the same nonlinear Celsius palette used by the thermal-imager legend (-100, -50, 0, +50, +1200 C anchors), with a modest matching glow so the eye catches current temperature immediately
  - keep T_f neutral/muted to preserve the visual distinction between observation and forecast
  - update Planet-panel terminology to 'Текущая Tₐ поверхности' and 'Ожидаемая T_f режима'
  - refresh T_a directly on Weather Core diagnostic updates and at 250 ms telemetry cadence without coupling temperature UI to render FPS physics
Regression: thermal-celsius.test.js now requires separate T_a/T_f sources, immediate Weather Core initialization, forecast labelling, and reuse of the thermal-imager colour scale.
Release rule: every new archive increments patch version by 0.0.1 and is named madPlanet-X.Y.Z.zip.
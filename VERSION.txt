VERSION 0.5.111
Base: 0.5.110
Changes: Keep the pause and hamburger controls clear of the bottom utility panel on portrait devices.
  - on portrait tablets wider than 700 CSS px, lift both bottom-corner buttons by 58 px so they sit just above the desktop-style utility bar instead of covering it
  - preserve the existing left/right corner placement: pause stays bottom-left and hamburger stays bottom-right
  - on narrow phones, keep the hamburger at its existing top-center position and lift pause by 102 px to clear both the bottom rubric row and the horizontally scrolling utility bar
  - leave landscape/desktop pause placement unchanged
Regression: extend touch-ux.test.js with layout guards for tablet and phone bottom-control clearance.
Snapshot: 0.5.110 remains the rollback point before this responsive control-placement adjustment.
Release rule: every new archive increments patch version by 0.0.1 and is named madPlanet-X.Y.Z.zip.

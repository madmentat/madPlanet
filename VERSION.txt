VERSION 0.5.155
Base: 0.5.154, with the visible-river contract recovered from 0.5.148.
Changes: narrow, curved, coast-safe rivers.
  - The cubemap is a one-texel permission corridor again; it is never painted directly as water and is never dilated by max footprint taps.
  - Whole-chain Chaikin/displacement is retired. Four-cell Catmull edges use deterministic C1 node-anchored meanders and the strongest upstream tangent.
  - Conservative 0.5.148 source/discharge density is restored while keeping the exact CPU/GPU terrain match, coast-distance gate, basin lock, downhill routing and first-ocean stop.
  - Dead, unbundled RiverValidation/RiverSourceFilter stubs are removed.
Regression: numeric guards cover maximum corridor brush width and zero meander displacement at graph nodes.
Release rule: every new archive increments patch version by 0.0.1 and is named madPlanet-X.Y.Z.zip.

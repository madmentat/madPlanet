VERSION 0.5.163
Base: 0.5.162 fine rivers.
Changes: restore reliable weather telemetry and visible low-cloud baseline on mobile/desktop.
  - T_a now has a direct Weather Core area-weighted fallback when the climate-consistency adapter temporarily has no matching core.
  - The fallback is installed before celsius-ui, so the existing single-writer telemetry path remains authoritative.
  - Low-cloud visual influence keeps a display-only floor at -0.55; physical Weather Core fields remain unchanged. This prevents the inertial visual response from suppressing the default low cloud deck completely on dry worlds while preserving regional variation.
  - No river, terrain or physical weather fields are changed by the visual floor.
Regression: T_a must resolve to a finite Celsius value after Weather Core creation; low-cloud influence must never be packed below the display floor.
Release rule: every new archive increments patch version by 0.0.1 and is named madPlanet-X.Y.Z.zip.

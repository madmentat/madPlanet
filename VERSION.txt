VERSION 0.5.87b
Base: 0.5.87
Changes: Residual curved stripes persisted after the 280-pairCompetitive + reduced-bump changes. The decisive fix is a hard spatial gate on the physical tectonic relief.
After tectonicBelt() the height/lee contributions are multiplied by
  seamGate = exp(-28.0 * gSeamNear)
where gSeamNear is the normalised distance to the real nearest/second-nearest Voronoi boundary. Deep inside a plate the gate collapses, eliminating interior ghost arcs while leaving genuine boundary-adjacent mountains intact.
Validation: seed 90636862, Tectonics 0%->100%, Plates on/off. Interior mathematical arcs must disappear; real plate edges and orographic belts remain.
Snapshots: snapshot/0.5.86 is the pre-0.5.87 rollback. Create snapshot/0.5.87b after visual confirmation.
Release rule: every new archive increments patch version by 0.0.1 and is named madPlanet-X.Y.Z.zip.

VERSION 0.5.156
Base: 0.5.155, retaining the narrow coast-safe river geometry recovered from 0.5.148.
Changes: stable river visibility across camera distance without widening channels.
  - WebGL2 uses mipmapped minification for the river corridor cubemap, preserving sub-pixel coverage instead of producing dotted or missing lines at orbit scale.
  - Physical rivers receive a bounded opacity floor after the existing one-pixel morphology calculation; procedural rivers keep their legacy area-correct fade.
  - No direct corridor painting, footprint max-dilation, Chaikin chain painter or trunkLine water path is reintroduced.
Regression: guards require mipmapped minification, a bounded physical LOD floor and the existing one-texel corridor limits.
Release rule: every new archive increments patch version by 0.0.1 and is named madPlanet-X.Y.Z.zip.

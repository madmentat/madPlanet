VERSION 0.5.93
Base: 0.5.92
Changes: Island silhouettes were still razor-cut by a thin vertical line (seed 8127344) even at Tectonics=0. One-sided finite differences along an explicit tangent frame could invent a one-sided cliff when neighbouring height samples disagreed.
Fix in surface.glsl normals:
  1) Frisvad orthonormal basis (continuous except the single south pole)
  2) central differences (±eps on both axes) instead of one-sided +eps samples
localTectSupport bump gate retained. Pause/zoom behaviour unchanged.
Validation: seed 8127344, same camera; the island must no longer be truncated by a vertical line at Tectonics 0% and 100%.
Snapshots: snapshot/0.5.92-pre-dfdx-normals is the rollback point.
Release rule: every new archive increments patch version by 0.0.1 and is named madPlanet-X.Y.Z.zip.

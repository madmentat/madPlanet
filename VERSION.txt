VERSION 0.5.150
Base: 0.5.149
Changes: river validation layer.
  - added non-invasive river graph diagnostics
  - downhill monotonicity checks for downstream paths
  - ocean re-entry detection after river mouth
  - basin consistency checks
  - network statistics collection for future Horton-Strahler filtering
  - validation does not modify hydrology output; it only reports invalid paths
Snapshot: 0.5.149 remains the rollback point before validation diagnostics.
Release rule: every new archive increments patch version by 0.0.1 and is named madPlanet-X.Y.Z.zip.

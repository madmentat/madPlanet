VERSION 0.5.143
Base: 0.5.142
Changes: Kill D8 staircases and make rivers read as smooth natural streams.
  - river paths no longer snap through Weather Core cell centres with zero endpoint offset
  - meander amplitude is a fraction of synoptic cell size, not sin of the tiny inter-cell angle
  - residual lateral offset at segment ends breaks grid polylines / ladder corners
  - denser edge sampling and higher river cubemap upscale (16x) for continuous corridors
  - multi-harmonic continuous-phase bends on trunks and visual tributaries
  - thinner paint radii with softer falloff so channels stay fine without blocky fills
  - denser basin-constrained feeders/headwaters retained from the display polish
  - physical Priority-Flood / discharge / downstream graph unchanged; display only
Regression: visual inspection at high zoom must not show orthogonal D8 ladders; rivers should meander smoothly between drainage cells.
Release rule: every new archive increments patch version by 0.0.1 and is named madPlanet-X.Y.Z.zip.
